# Copy to .env for local use (do NOT commit .env)
# On Streamlit Cloud, set these under App settings → Secrets instead.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true
EMAIL_FROM=
EMAIL_DEMO_MODE=true

ANOMALY_CONTAMINATION=0.05
